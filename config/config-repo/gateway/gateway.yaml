# ==========================================
# GATEWAY ROUTING & EDGE CONFIGURATION
# ==========================================

server:
  port: 8443
  ssl:
    client-auth: none

spring:
  # == Redis Session Management ==
  session:
    timeout: 7d
    data:
      redis:
        namespace: "transcendence"
        save-mode: on-set-attribute
        repository-type: indexed
  data:
    redis:
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}

  # == Security & OAuth2 Configuration ==
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/google"

  # == WebClient & Netty Routing Engine Client SSL Configuration ==
  cloud:
    gateway:
      server:
        webflux:
          httpclient:
            wiretap: true
            ssl:
              ssl-bundle: microservice-bundle
        # == Discovery Configuration ==
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true

          routes:
            # Config Server Proxy (Internal only)
            - id: config-server-proxy
              uri: https://config-server:8888
              order: 0
              predicates:
                - Path=/config-server/**
              filters:
                - StripPrefix=1

            # Eureka Server Proxy (Internal only)
            - id: eureka-server-proxy
              uri: https://eureka-server:8761
              order: 0
              predicates:
                - Path=/eureka-server/**
              filters:
                - StripPrefix=1

            # OpenAPI Spec Bypass (High Priority)
            - id: auth-service-openapi-bypass
              uri: lb://auth-service
              order: 1
              predicates:
                - Path=/api/auth/v3/api-docs/**, /api/auth/v3/api-docs
              filters:
                - StripPrefix=2

            # Public paths
            - id: auth-service-public
              uri: lb://auth-service
              order: 2
              predicates:
                - Path=/api/auth/login, /api/auth/register
              filters:
                - StripPrefix=2

            # Semi-Protected (2FA challenge)
            - id: auth-service-2fa-verification
              uri: lb://auth-service
              order: 3
              predicates:
                - Path=/api/auth/2fa/verify
              filters:
                - StripPrefix=2
                - SessionToJwt

            # Secure Catch-All (Enforces Full Authentication)
            - id: auth-service-secure-catchall
              uri: lb://auth-service
              order: 4
              predicates:
                - Path=/api/auth/**
              filters:
                - StripPrefix=2
                - SessionToJwt
                - TwoFactorCheck

            # OpenAPI Spec Bypass for Social Service
            - id: social-service-openapi-bypass
              uri: lb://social-service
              order: 5
              predicates:
                - Path=/api/social/v3/api-docs/**, /api/social/v3/api-docs
              filters:
                - StripPrefix=2

            # Secure Route for Social Service (Enforces Full Authentication & 2FA Check)
            - id: social-service-secure
              uri: lb://social-service
              order: 6
              predicates:
                - Path=/api/social/**
              filters:
                - StripPrefix=2
                - SessionToJwt
                - TwoFactorCheck

# ==========================================
# OPENAPI / SWAGGER AGGREGATION CONFIGURATION
# ==========================================
springdoc:
  swagger-ui:
    use-root-path: false
    urls:
      - name: "Authentication Service"
        url: "/api/auth/v3/api-docs"
      - name: "Social Service"
        url: "/api/social/v3/api-docs"

# ==========================================
# APPLICATION CORE SECURITY & CORS CONFIG
# ==========================================
app:
  cors:
    allowed-origins: "${CORS_ALLOWED_ORIGINS:http://localhost:5173}"
  frontend:
    base-url: "${FRONTEND_BASE_URL:http://localhost:5173}"

logging:
  level:
    org.springframework.cloud.gateway: TRACE
    org.springframework.cloud.loadbalancer: TRACE

