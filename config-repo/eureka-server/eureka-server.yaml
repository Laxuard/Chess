# ==========================================
# Eureka Server Configuration
# ==========================================

server:
  port: ${EUREKA_PORT}

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false