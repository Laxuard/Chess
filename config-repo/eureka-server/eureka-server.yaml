# ==========================================
# Eureka Server Configuration
# ==========================================

server:
  port: ${EUREKA_PORT}
  ssl:
    client-auth: want

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false